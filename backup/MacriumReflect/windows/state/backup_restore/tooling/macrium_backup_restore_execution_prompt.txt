$until-done $tdd-workflow $verification-loop $research-after-failure $no-deferred-work $security-review
You are working on Windows from F:\downloads. Create, save, and test a Python script that backs up the current perfect working state of Macrium Reflect and later restores from the latest backup stored in F:ackup\windowsapps\AppsBackups. Treat this as a real local operations task, not a suggestion or outline. Do not stop at analysis, a plan, pseudocode, a partial script, or an untested claim. Continue until a working script exists, the backup path exists, a backup artifact can be created, restore discovery works, safety gates work, and verification evidence is available. If you cannot complete a required step because of a real external blocker, report only that blocker, the exact missing input or access, what you already proved, and the next command the user must run.

The user wants a Python tool that captures Macrium Reflect exactly as it is now across all relevant drives and paths, saves the backup file under F:ackup\windowsapps\AppsBackups, and can use the newest backup file in that folder to restore Macrium Reflect so it works just like the latest backed-up state. Interpret this conservatively and safely: back up Macrium Reflect application files, configuration, definition files, logs where useful, registry settings, scheduled task metadata, service state metadata, shortcuts, license-related metadata when legally accessible, and any Macrium-related folders discovered on available local drives. Do not try to clone whole disks or image every drive unless the user explicitly asks for full disk imaging. Do not perform a destructive restore automatically. Restore mode must require an explicit command-line flag and an additional confirmation mechanism unless the restore target is a harmless temporary verification directory.

Your deliverable is a real Python script saved in the current workspace, preferably named macrium_state_backup_restore.py unless an existing project convention demands another name. If helper files are needed, keep them minimal and explain them. The script must be usable from Windows PowerShell. It must support at least these commands: inventory, backup, latest, verify, restore, and restore-dry-run. It must accept the backup root path, defaulting to F:ackup\windowsapps\AppsBackups. It must create a timestamped backup package per run and maintain a latest marker or deterministic latest selection. It must not overwrite old backups. It must write a manifest with every captured source, destination inside the archive/package, size, hash where practical, timestamps, and any items that were skipped with a reason. It must write logs that make failures diagnosable.

Before implementing, inspect the local Windows environment with safe read-only commands. Discover whether Macrium Reflect is installed, where its executable lives, whether Macrium services exist, what scheduled tasks contain Macrium or Reflect names, what registry locations are present, and what Macrium-related directories exist under common paths and on available fixed drives. Common examples to check include Program Files, Program Files (x86), ProgramData, AppData for the current user, Public Desktop, Start Menu folders, Desktop shortcuts, Documents folders, and any Macrium Reflect definition file locations. Use the registry only through read-only operations during discovery. Do not assume paths from generic knowledge when they can be discovered locally. If Macrium is not installed or cannot be found, create a script that reports that cleanly and exits nonzero for backup attempts, then report the exact evidence.

Design the script for robust Windows behavior. Use pathlib, subprocess, shutil, zipfile or tarfile, json, hashlib, argparse, datetime, tempfile, fnmatch, re, and winreg where appropriate. Avoid third-party dependencies unless they are already installed and genuinely needed. Prefer standard library. Handle long paths carefully. Preserve relative path structure inside the backup package. Avoid following symlink or junction loops. Detect inaccessible files and record them rather than crashing the whole backup. Use safe copy behavior. Prefer copy2 for metadata when copying to a staging folder. Compute SHA256 for normal files when size is reasonable; for very large files, either stream hashes or record why hashing was skipped. Never print secrets intentionally. Redact obvious license keys or secret-looking registry values in human logs while still preserving allowed raw export files only if that is legally and technically appropriate.

Backup format must be restorable. A good default is a timestamped folder containing manifest.json, inventory.json, restore_plan.json, logs, registry_exports, task_exports, and files, plus a compressed zip package if feasible. If a single file is required by the user's wording, produce a .zip backup file in F:ackup\windowsapps\AppsBackups and keep any temporary staging folder only until the zip is verified. The newest successful backup must be detectable by manifest status and timestamp, not merely by filename if a failed partial package exists. The verify command must validate that the zip opens, the manifest is present, selected hashes match, required metadata files exist, and the package is internally consistent.

Restore behavior is safety-critical. Implement restore-dry-run first and make it the default recommended restore path. It must read the newest valid backup from F:ackup\windowsapps\AppsBackups, print the exact restore plan, list destination paths, identify files that would be overwritten, list registry imports that would be required, list services or tasks that may need restart or reimport, and refuse to change the system. The real restore command must require explicit flags such as --execute-restore and --i-understand-this-can-overwrite-files, plus an admin check when writing protected paths or importing registry data. If not elevated, it must fail safely with a clear message. It must create a pre-restore safety snapshot of overwritten files where practical before replacing them. It must never delete unrelated files. It must not restore into a path outside the manifest-defined destinations unless the user explicitly provides a relocation target for testing.

Testing is mandatory. Use a verification ladder. First run static syntax checks such as python -m py_compile on the script. Then run inventory and confirm it produces structured output without crashing. Then run a backup into F:ackup\windowsapps\AppsBackups or, if the real path is unavailable, create the real path if possible; if permissions block it, use a temporary backup root only as a fallback and report that the real path was blocked. Then run latest and verify it selects the newest successful backup. Then run verify against that backup. Then run restore-dry-run against the latest backup and confirm it prints a restore plan without changing files. Then test a harmless restore path if your script supports relocation or temporary-target restore; do not perform a real destructive system restore without the user's explicit approval.

If any test fails, do not stop. Read the exact error, diagnose the cause, modify the script, and rerun the relevant check plus any broader checks that could regress. After two failed attempts on the same issue, stop guessing and research the exact failure locally and, if the failure depends on current Macrium, Windows, or Python behavior, use primary documentation or official sources. Keep notes of the attempts and the final reason the fix works. Do not hide failures by weakening tests. Do not mark a backup successful unless verification passes.

The script must be user-friendly. Provide --help text with examples. Include examples for: inventory, backup using default root, verify latest, restore dry-run latest, and real restore with explicit dangerous flags. Error messages should say what happened, what path or registry key was involved, and what the user can do next. The script should return meaningful exit codes: 0 for success, nonzero for failure, and a distinct nonzero code for blocked admin-required restore when practical. Avoid interactive prompts for normal backup and verify commands. For dangerous restore, use both command-line flags and an optional typed confirmation unless --yes-dangerous-restore is provided.

Security and integrity requirements are not optional. Validate that the backup root resolves under F:ackup\windowsapps\AppsBackups by default. Prevent path traversal when extracting from zip. Validate archive members before writing. Reject manifests with absolute extraction paths unless they match an explicitly intended destination and pass safety checks. Preserve a clear separation between package contents and live restore destinations. Use atomic or staged writes where possible. Write partial backups with a status such as incomplete until all copy and verification steps finish, then mark them successful. Do not silently continue after manifest corruption. Do not import registry files blindly during normal dry-run.

Acceptance criteria that must all pass before you may claim completion: the Python script exists in the workspace; F:ackup\windowsapps\AppsBackups exists or a specific permission blocker is proven; inventory command runs; backup command creates a timestamped backup artifact; the artifact contains a manifest and discovered Macrium-related content or a clear explanation that none exists; latest command selects the newest successful artifact; verify command validates that artifact; restore-dry-run command loads that artifact and prints a safe restore plan; the script protects against path traversal and accidental destructive restore; command help is understandable; all verification commands and their outcomes are reported. Do not leave TODO, FIXME, placeholder restore logic, or fake test output.

Work style: plan briefly before editing because this crosses filesystem, registry metadata, backup format, and safety gates. Then implement. Keep changes focused. Prefer direct command-backed evidence over assumptions. If the workspace already has related scripts, inspect them and avoid breaking them. If there are unrelated dirty files, do not revert them. Use exact paths in commands and reports. When making file edits, preserve ASCII unless the file already uses non-ASCII. Keep comments rare and only where the safety logic would otherwise be hard to audit.

Final response must be compact but proof-heavy. Include the script path, the backup root used, the exact commands run, whether each passed or failed, the latest backup artifact path, what restore-dry-run proved, and any residual uncertainty. If real restore was not executed because it would overwrite live system state and needs user approval, say that clearly and provide the exact safe command for the user to run only if they approve. Do not claim Macrium Reflect was perfectly restored unless a real restore was explicitly approved and verified. Do not claim a perfect backup if key Macrium paths were inaccessible; instead report which paths were skipped and why.

Implementation details to strongly consider: create functions for logging, admin detection, path normalization, safe archive member validation, inventory discovery, registry export or registry read capture, scheduled task export, service metadata capture, file collection, manifest writing, zip creation, zip verification, latest backup selection, restore plan generation, dry-run reporting, and guarded restore execution. Keep command parsing clear. Use JSON for machine-readable metadata. Name backup packages with local timestamp and a stable prefix such as macrium-reflect-state-YYYYMMDD-HHMMSS.zip. Include the hostname and username in metadata only if useful for restore clarity. Use subprocess with explicit argument arrays, not shell=True, except where a Windows built-in requires it and you can justify it.

Do not use dangerous shortcuts. Do not copy all of C:\ blindly. Do not delete existing backups. Do not run registry imports or overwrite Program Files during testing without explicit user approval. Do not assume Macrium Reflect's licensing terms permit copying license secrets; handle license metadata conservatively and report limitations. Do not treat successful zip creation alone as sufficient. Do not treat a dry-run as a completed real restore. Do not leave the user with a script that only works on your machine by accident; make paths configurable while preserving the requested default.

Start now by producing a short execution plan, then inspect, implement, and verify. Keep going until every acceptance criterion is either passed with command-backed proof or blocked by a concrete external requirement that you name precisely.
When inventorying drives, distinguish fixed local drives from removable, network, optical, and unavailable volumes. Scan only likely Macrium-related names unless the user approves an expensive full-drive crawl. Record the scan strategy in inventory.json so the backup scope is auditable later.
Prefer a manifest schema that includes schema_version, created_at, tool_version, machine, backup_root, package_path, status, sources, registry, scheduled_tasks, services, skipped_items, warnings, and verification. This makes future restores less dependent on fragile filename parsing.
For registry data, capture both machine-level and user-level Macrium-looking keys when present. If reg.exe export is used, call it with explicit arguments and store exports under registry_exports. If only winreg reads are used, serialize values to JSON and mark binary values safely.
For scheduled tasks, use schtasks query or PowerShell export only as read-only discovery during backup. Store task XML when accessible. During restore-dry-run, list task imports as planned actions, but do not register tasks unless dangerous restore flags and elevation are present.
For services, record display name, service name, executable path, start type, current state, and dependencies when discoverable. Do not stop or restart services during backup unless the user explicitly approves. Restore-dry-run may recommend a restart, but must not perform it.
Build the latest-selection rule carefully: ignore incomplete manifests, corrupt zips, temp files, and packages whose manifest status is not success. If no valid backup exists, fail with a clear message and show the backup command needed to create one.
Use dry-run output as a contract. It should clearly separate file restores, registry restores, task restores, service notes, conflicts, required elevation, and commands that would be run. It should be possible for the user to review it before approving a real restore.
For real restore, create a pre_restore folder under the backup root or a user-selected safety folder containing copies of files that would be overwritten, plus a rollback manifest. If that safety copy cannot be made, refuse the destructive restore.
Add a --backup-root option for all commands that read or write backups. Normalize it once, create it for backup if allowed, and make every command print the resolved path so the user can see whether F:\backup\windowsapps\AppsBackups is actually being used.
If the machine has multiple Python launchers, use the interpreter running the script for subprocess examples when practical. In the final report, show commands in a form the user can paste into PowerShell, such as python .\macrium_state_backup_restore.py backup.
Keep test artifacts intentional. Do not leave large temporary staging folders after a successful zip verification. If cleanup fails, report the path and why. Never clean unknown folders by broad wildcard deletion.
If backup size is large, stream file reads and writes instead of loading whole files into memory. Log progress enough to prove the script is active, but avoid noisy output that hides real errors.
Make restore extraction safe before implementing overwrite behavior. Check every archive member for absolute paths, drive prefixes, parent traversal, alternate data stream tricks, and destination escape from the intended staging directory. Reject unsafe packages.
Report manifest schema version.
Report Python version used.
Report whether elevation was present.
Report whether Macrium services were found.
Report final package byte size.
F